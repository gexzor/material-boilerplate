// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: 'AIzaSyBIPia3tTxSm1t2TPFb14s1tzkgF94tJAA',
        authDomain: 'material-boilerplate.firebaseapp.com',
        databaseURL: 'https://material-boilerplate.firebaseio.com',
        projectId: 'material-boilerplate',
        storageBucket: 'material-boilerplate.appspot.com',
        messagingSenderId: '209917247741',
        appId: '1:209917247741:web:7d4a15b88ce47d525d7e94',
    },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
